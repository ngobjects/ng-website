package ng.website.components;

import ng.appserver.NGComponent;
import ng.appserver.NGContext;

public class DocumentationPage extends NGComponent {

	public DocumentationPage( NGContext context ) {
		super( context );
	}
}